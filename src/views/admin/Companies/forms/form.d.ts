declare namespace Models {
    export interface FormCreateCompanyAdmin {
        // rut, razon_social, direccion, telefono, logo, logo_format
        razon_social: string
        direccion: string
        telefono: string
        logo: string
        logo_format: string
    }
}
declare namespace Models {
    export interface InsumoFormModel {
        // title, description, marca, modelo, nro_componente, stock, year, image, format_image
        title: string
        description: string
        marca: string
        modelo: string
        nro_componente: number
        stock: number
        year: number
        image: string
        format_image: string
    }
}